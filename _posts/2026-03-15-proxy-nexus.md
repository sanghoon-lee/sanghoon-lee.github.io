---
layout: post
title: "폐쇄망 개발환경의 내부 라이브러리 저장소 아키텍처 구성"
date: 2026-03-15
description: 내부 라이브러리를 구성하는 방법을 아키텍처 중심으로 설명한 글입니다. 
image: /assets/images/nexus.jpg
categories: 개념정리
---

대부분의 개발자는 오픈소스 라이브러리를 사용하는 것에 대해 크게 고민할 필요가 없습니다.

Gradle이나 Maven과 같은 빌드 도구가 공개된 라이브러리 저장소에 접근해서 필요한 라이브러리를 알아서 가져오기 때문입니다.

개발자는 빌드 도구의 설정에 필요한 라이브러리 정보만 등록해두면 됩니다. 하지만 인터넷이 차단된 폐쇄망 네트워크에서는 이렇게 편리한 빌드 도구의 기능을 활용하기 어렵습니다.

---

## 1. 일반적인 개발환경과 폐쇄망 개발환경의 차이

인터넷 사용이 자유로운 일반적인 개발환경에서는 개발자의 PC나 CI 서버에서 실행되는 빌드 도구가 공개된 라이브러리 저장소로부터 빌드에 필요한 라이브러리를 다운로드 받게 됩니다.

```nohighlight
개발자 PC / CI 서버
       ↓
외부의 공개 저장소(예: Maven Central)
       ↓
Dependency Download
```

하지만, 폐쇄망 네트워크를 사용하는 개발환경에서는 상황이 완전히 달라집니다. 인터넷이 차단되어 있기 때문에 외부에 있는 라이브러리 저장소로부터 필요한 라이브러리를 다운로드 받는 것이 불가능합니다.

```nohighlight
개발자 PC / CI 서버
       ↓
     (차단)
       ↓
외부의 공개 저장소(예: Maven Central)
```

그러면 자연스럽게 다음과 같은 질문이 생깁니다.

> 폐쇄망 네트워크를 사용한 환경에서는 어떻게 빌드에 필요한 라이브러리를 가져올 수 있을까요?

---

## 2. 폐쇄망 개발환경에 라이브러리를 수동으로 반입하는 방식의 문제점

가장 단순한 방법은 인터넷 사용이 자유로운 환경에서 필요한 라이브러리를 다운로드하고, 수동으로 폐쇄망으로 반입하는 것입니다.

다음과 같은 방식입니다.

```nohighlight
1. 인터넷 사용이 가능한 환경에서 라이브러리 다운로드
2. 라이브러리 파일을 폐쇄망으로 반입 요청
3. 보안 담당자 확인 및 승인
4. 반입된 라이브러리 파일을 프로젝트에 직접 추가하거나 내부 저장소에 업로드
```

하지만 이런 방식을 사용하면 다음과 같은 문제가 발생할 수 있습니다. 

---

### 2.1. 라이브러리 반입 과정의 번거로움

대부분의 라이브러리는 또 다른 라이브러리들과 복잡한 의존관계를 형성하고 있는 경우가 대부분입니다. 

따라서 의존성까지 고려해 어떤 라이브러리들을 함께 가져와야 하는지 파악해야 합니다.

이 과정은 생각보다 복잡하며 매번 반복되어야 합니다. 결과적으로 개발 생산성 저하로 이어질 수 있습니다.

---

### 2.2. 버전 관리의 어려움

또한 어떤 버전의 라이브러리가 조직 내에서 사용되고 있는지 추적하는 것이 어렵습니다. 

그래서 이미 과거에 반입된 라이브러리임에도 불구하고, 다른 팀이나 프로젝트에서는 이를 알지 못해 다시 반입하는 일이 발생하기도 합니다.

동일한 라이브러리를 팀마다 서로 다른 버전으로 사용하고 있거나, 라이브러리를 직접 프로젝트 내부에 포함시켜서 유지보수를 어렵게 만들기도 합니다. 

---

## 3. 폐쇄망 개발환경에 내부 저장소 구축이 필요한 이유

위와 같은 문제를 해결하기 위해서 결국 내부로 반입된 라이브러리를 통제하고, 관리할 수 있는 별도의 방법이 필요합니다.

그래서 폐쇄망에서는 대부분 **내부 저장소(Artifact Repository)**를 구축해서 운영하고 있습니다.

대표적인 도구로 **Nexus**가 있습니다.

<img class="main-image" src="/assets/images/nexus.png" alt="nexus repository">

**Nexus**를 통해 **내부 저장소**를 구축하면 반입된 라이브러리를 중앙에서 관리하고, 조직 내에서 일관된 방식으로 사용할 수 있습니다.

개발자는 일반적인 개발환경과 동일하게 라이브러리 정보를 빌드 도구의 설정에 추가하면 됩니다.

하지만 공개 저장소가 아닌 **내부 저장소**를 바라보도록 설정해야 한다는 차이가 있습니다. 

---

## 4. 프록시 서버의 활용

하지만 **내부 저장소**가 구축되더라도 여전히 폐쇄망에서 외부의 공개 저장소에는 접근할 수 없습니다.

그렇기 때문에 라이브러리는 여전히 수동으로 반입되어야 합니다. 일반적인 환경과 동일하게 라이브러리 반입까지 자동화할 수 없을까요?

라이브러리 반입을 자동화하려면 결국 **내부 저장소**에서 외부의 공개 저장소에 접근이 가능해야 합니다.

폐쇄망을 사용하는 많은 조직에서 프록시 서버(Forward Proxy)를 활용해서 이 문제를 해결하고 있습니다. 

**내부 저장소**가 프록시 서버를 경유하여 허용된 경로로 안전하게 외부와 통신할 수 있도록 통제하는 것입니다.

이러한 구조를 그림으로 나타내면 다음과 같습니다.

```nohighlight
개발자 PC / CI 서버
       ↓
내부 저장소 - 폐쇄망(Trust Zone)
       ↓
프록시 서버 - 폐쇄망(DMZ)
       ↓ 
외부의 공개 저장소(예: Maven Central) - 인터넷 
       ↓
Dependency Download
```

---

## 5. 관련 포스팅

* [[학습기록] Nexus로 내부 라이브러리 저장소 구축해보기](https://sanghoon-lee.github.io/2026/03/10/localNexus/)

---

#nexus #proxy #폐쇄망 #저장소 #라이브러리