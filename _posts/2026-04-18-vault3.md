---
layout: post
title: "[토이프로젝트] Vault를 이용한 KMS 대체 검토: (4) Java 애플리케이션에서 Vault Transit Engine 연동하기"
date: 2026-04-18
description: HashiCorp Vault의 Transit Engine과 연동하는 Java 애플리케이션을 구현하는 방법에 대해서 정리한 글입니다.
image: /assets/images/hashicorp.jpg
categories: 토이프로젝트
tags:
  - 보안
  - 인프라
---

지금까지 KV Engine과 Transit Engine을 직접 실습해보면서 Vault가 제공하는 주요 기능들을 확인했습니다.

앞선 실습은 CLI를 사용해 진행할 수 있었지만, 실제 시스템에서는 애플리케이션이 API를 호출하는 방식으로 Vault와 연동해야 합니다.

그래서 이번에는 간단한 Java 애플리케이션을 구현하면서 Transit Engine을 API로 호출하고 연동하는 방법을 알아보도록 하겠습니다.

---

## 1. 전체 코드

아래 예제는 `hello,vault`라는 문자열을 Transit Engine으로 암호화하고, 다시 복호화를 하는 동작을 수행하는 코드입니다. 

스프링의 RestClient, WebClient 또는 Vault 전용 라이브러리를 활용하여 보다 편리하게 연동할 수도 있지만, Vault API의 동작 방식을 이해하기 위해 Java의 HttpClient를 사용해 직접 요청을 생성했습니다.

또한 예제의 복잡도를 낮추기 위해 문자열 파싱으로 응답값을 추출했습니다. 실제 서비스를 구현할 때는 Jackson과 같은 JSON 라이브러리를 사용하는 것이 일반적입니다.

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.util.Base64;

public class VaultConsoleExample {
    public static void main(String[] args) {
        String vaultAddr = "http://192.168.50.69:8200"; // Vault 주소는 환경에 맞게 변경
        String token = "root";

        try {
            HttpClient client = HttpClient.newHttpClient();

            // =========================
            // 1. 암호화
            // =========================

            String plaintext = "hello,vault";
            String encoded = Base64.getEncoder().encodeToString(plaintext.getBytes());

            String encryptJson = "{ \"plaintext\": \"" + encoded + "\" }";

            HttpRequest encryptRequest = HttpRequest.newBuilder()
                    .uri(URI.create(vaultAddr + "/v1/transit/encrypt/test-key"))
                    .header("X-Vault-Token", token)
                    .header("Content-Type", "application/json")
                    .POST(HttpRequest.BodyPublishers.ofString(encryptJson))
                    .build();

            HttpResponse<String> encryptResponse =
                    client.send(encryptRequest, HttpResponse.BodyHandlers.ofString());

            if (encryptResponse.statusCode() != 200) {
                System.out.println("암호화 실패");
                System.out.println(encryptResponse.body());
                return;
            }

            System.out.println("암호화 성공");
            System.out.println(encryptResponse.body());

            // 간단하게 ciphertext 추출 (문자열 파싱)
            String ciphertext = extractValue(encryptResponse.body(), "ciphertext");

            // =========================
            // 2. 복호화
            // =========================

            String decryptJson = "{ \"ciphertext\": \"" + ciphertext + "\" }";

            HttpRequest decryptRequest = HttpRequest.newBuilder()
                    .uri(URI.create(vaultAddr + "/v1/transit/decrypt/test-key"))
                    .header("X-Vault-Token", token)
                    .header("Content-Type", "application/json")
                    .POST(HttpRequest.BodyPublishers.ofString(decryptJson))
                    .build();

            HttpResponse<String> decryptResponse =
                    client.send(decryptRequest, HttpResponse.BodyHandlers.ofString());

            if (decryptResponse.statusCode() != 200) {
                System.out.println("복호화 실패");
                System.out.println(decryptResponse.body());
                return;
            }

            System.out.println("복호화 성공");
            System.out.println(decryptResponse.body());

            // Base64 plaintext 추출
            String decodedBase64 = extractValue(decryptResponse.body(), "plaintext");

            // Base64 → 원문 문자열
            String result = new String(Base64.getDecoder().decode(decodedBase64), StandardCharsets.UTF_8);

            System.out.println("최종 복호화 결과: " + result);

        } catch (Exception e) {
            System.out.println("Vault 호출 중 오류 발생");
            e.printStackTrace();
        }
    }

    // 간단한 JSON 값 추출 (예제용)
    // 실무에서는 JSON 파싱 라이브러리(Jackson 등)를 사용하는 것이 좋습니다.
    // 특히 응답 구조가 변경될 수 있기 때문에, 문자열 파싱 방식은 유지보수에 취약할 수 있습니다.
    private static String extractValue(String json, String key) {
        String pattern = "\"" + key + "\":\"";
        int start = json.indexOf(pattern) + pattern.length();
        int end = json.indexOf("\"", start);
        return json.substring(start, end);
    }
}
```

---

## 2. 코드 설명

예제 코드는 평문을 암호화한 뒤 다시 복호화하여 원본 문자열을 복원하는 흐름으로 구성했습니다.

전체적인 동작 흐름은 다음과 같습니다.

```nohighlight
평문 문자열
    ↓
Base64 인코딩
    ↓
Vault 암호화 API 호출
    ↓
암호문 수신
    ↓
Vault 복호화 API 호출
    ↓
Base64 문자열 수신
    ↓
Base64 디코딩
    ↓
원본 문자열 복원
```

---

### 2.1. 인코딩/디코딩

**암호화: 평문을 Base64로 인코딩**
```java
String encoded = Base64.getEncoder().encodeToString(plaintext.getBytes());
```

Vault의 Transit Engine은 입력값을 Base64 형태로 받기 때문에, 암호화를 요청하기 전에 반드시 인코딩 과정이 필요합니다.

> Vault는 내부적으로 바이너리 데이터를 처리하기 때문에, 문자열을 그대로 전달하면 오류가 발생할 수 있습니다. 

**복호화: 복호화된 결과를 Base64로 디코딩**
```java
String result = new String(Base64.getDecoder().decode(decodedBase64), StandardCharsets.UTF_8);
```

복호화 결과는 Base64 형태로 반환되기 때문에, 애플리케이션에서는 디코딩 과정을 통해 원본 문자열을 복원해야 합니다.


---

### 2.2. Vault API 호출

Vault는 별도의 전용 프로토콜을 사용하는 것이 아니라, HTTP 기반의 REST API로 동작합니다.

```nohighlight
애플리케이션 → HTTP 요청 → Vault → 결과 반환
```

따라서 Java뿐만 아니라 Python, Go, Node.js 등 HTTP 통신이 가능한 모든 언어에서 동일한 방식으로 연동할 수 있습니다.


**암호화 요청 생성**
```java
HttpRequest encryptRequest = HttpRequest.newBuilder()
        .uri(URI.create(vaultAddr + "/v1/transit/encrypt/test-key"))
        .header("X-Vault-Token", token)
        .header("Content-Type", "application/json")
        .POST(HttpRequest.BodyPublishers.ofString(encryptJson))
        .build();
```

* /v1/transit/encrypt/test-key → 암호화 API 경로
* X-Vault-Token → 인증 토큰


**복호화 요청 생성**
```java
HttpRequest decryptRequest = HttpRequest.newBuilder()
        .uri(URI.create(vaultAddr + "/v1/transit/decrypt/test-key"))
        .header("X-Vault-Token", token)
        .header("Content-Type", "application/json")
        .POST(HttpRequest.BodyPublishers.ofString(decryptJson))
        .build();
```

* /v1/transit/decrypt/test-key → 복호화 API 경로
* X-Vault-Token → 인증 토큰

---

### 2.3. 응답 결과 확인

**암호화**
```java
System.out.println(encryptResponse.body());
```

Vault는 다음과 같은 형태로 암호화 결과를 반환합니다. data.ciphertext 값이 실제 암호화된 결과이며, Vault에서 관리하는 자체 포맷(vault:v1:...)으로 반환됩니다.

```json
{
  "data": {
    "ciphertext": "vault:v1:xxxxxxxx",
    "key_version": 1
  }
}
```

**복호화**
```java
System.out.println(decryptResponse.body());
```

Vault는 다음과 같은 형태로 복호화 결과를 반환합니다. data.plaintext 값이 실제 복호화된 결과(Base64로 인코딩된 값)입니다. 

```json
{
  "data": {
    "plaintext": "aGVsbG8sd29ybGQ="
  }
}
```

---

## 3. 마무리

Java 애플리케이션에서 API를 호출하여 Vault와 연동하는 방식을 살펴봤습니다.

Transit Engine뿐만 아니라 KV Engine과 같은 다른 기능들도 동일한 방식으로 연동할 수 있습니다.

이번 포스팅 시리즈를 통해 Vault의 주요 기능을 직접 사용해보면서 KMS 대체 가능성을 기술적으로 확인할 수 있었습니다.

다만 실제 KMS를 대체하려면 토큰 관리, 권한 제어, 장애 대응, 키 회전(Key Rotation)과 같은 운영 요소도 함께 고려되어야 합니다.

기술적인 측면에서는 Vault를 KMS 대안으로 활용할 수 있다는 점을 확인했지만, 실제 도입 여부는 운영 환경에 맞는 충분한 검토를 거쳐 결정해야 할 것입니다.

---

## 4. 포스팅 시리즈

* [[토이프로젝트] Vault를 이용한 KMS 대체 검토: (1) 개념정리와 설치과정](https://sanghoon-lee.github.io/2026/04/09/vault/)
* [[토이프로젝트] Vault를 이용한 KMS 대체 검토: (2) KV Engine을 이용한 암호화 키 저장과 조회 실습](https://sanghoon-lee.github.io/2026/04/10/vault1-1-rewriting/)
* [[토이프로젝트] Vault를 이용한 KMS 대체 검토: (3) Transit Engine을 이용한 암·복호화 실습](https://sanghoon-lee.github.io/2026/04/11/vault2/)
* [토이프로젝트] Vault를 이용한 KMS 대체 검토: (4) Java 애플리케이션에서 Vault Transit Engine 연동하기

---

## 5. 예제 소스 코드

* [VaultConsoleExample](https://github.com/sanghoon-lee/VaultConsoleExample)

